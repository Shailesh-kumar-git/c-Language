#include<stdio.h>

int main()
{
    int a,b,sum;
    printf("enter no.");
    scanf("%d %d", &a, &b); // inside this no statement needed
    sum=a+b;
    printf("sum of two numbers =%d",sum);

    return 0;
}